import React from 'react';

const Lobby: React.FC = () => {
  return (
    <div>
      <h1>Game Lobby</h1>
      <p>Create or join a game.</p>
      {/* Matchmaking UI will go here */}
    </div>
  );
};

export default Lobby;
